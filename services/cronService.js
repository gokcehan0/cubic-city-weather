// Cron service disabled - images are now permanent per city
// No daily regeneration needed

const startCronJobs = () => {
    // Cron disabled - images are permanent per city
};

module.exports = { startCronJobs };
